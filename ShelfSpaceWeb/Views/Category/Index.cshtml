@model List<Category>
<partial name="_Notification" />
<div class="container">
	<div class="row pt-4">
		<div class="col-6">
			<div class="text-primary fs-2 fw-bold text-black">Category List</div>
		</div>
		<div class="col-6 text-end">
			<a asp-controller="Category" asp-action="Create" class="btn btn-primary m-2">
				Create Category
				<i class="bi bi-file-earmark-plus"></i>
			</a>
		</div>
	</div>

</div>
<table class="table table-bordered table-striped">
	<thead>
		<tr> 
			<th>
				Categories
			</th>
			<th>
				Display Order
			</th><th></th>
		</tr>
	</thead>
	<tbody>
		@foreach (var cat in Model.OrderBy(el => el.DisplayOrder))
		{
			<tr>
				<td>@cat.Name</td>
				<td>@cat.DisplayOrder</td>
				<td>
					<div class="w-75 btn-group" role="group">
						<a asp-controller="Category" asp-action="Edit" asp-route-id="@cat.Id" class="btn btn-primary mx-2">
							<i class="bi bi-pencil-square"></i> Edit
						</a>
						<a asp-controller="Category" asp-action="Delete" asp-route-id="@cat.Id" class="btn btn-danger mx-2">
							<i class="bi bi-trash-fill"></i> Delete
						</a>
					</div>
				</td>
				
			</tr>
		}
	</tbody>
</table>